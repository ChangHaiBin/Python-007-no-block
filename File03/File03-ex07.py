
# Using the "is_prime" function from the previous question,
# Create another function, "sum_prime_numbers_until" function,
# that accepts an input K,
# and output the sum of all prime numbers from 1 to K

# e.g. when K = 7, the list of prime numbers are 2,3,5,7, and so
# sum_prime_numbers_until(7) = 2 + 3 + 5 + 7 = 17

# e.g. when K = 13, the list of prime numbers are 2,3,5,7,11,13 and so
# sum_prime_numbers_until(7) = 2 + 3 + 5 + 7 + 11 + 13 = 41

def is_prime(N):
    # ...................
    return True

def sum_prime_numbers_until(K):
    # .................
    return 100

print(sum_prime_numbers_until(7))
print(sum_prime_numbers_until(13))